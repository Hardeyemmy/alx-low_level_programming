#include <stdio.h>
/**
 *  main - Print upper and lower case alphabet
 *
 *  Return: End the program
 */
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
	{
		if (low = 'c' && low != 'q')
		{
			putchar(low);
		}
	}
	putchar("\n");
	return (0);
}
