#include <stdio.h>
/**
 *  main - Print upper and lower case alphabet
 *
 *  Return: End the program
 */
int main(void)
{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	{
		if (l = 'c' && l != 'q')
		{
			putchar(low);
		}
	}
	putchar('\n');
	return (0);
}
