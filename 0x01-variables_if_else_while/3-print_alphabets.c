#include <stdio.h>
/**
 *  main - Print upper and lower case alphabet
 *
 *  Return: End the program
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}

	for (c = 'A'; c <= 'Z' c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
