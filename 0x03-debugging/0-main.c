#include "main.h"
/**
 * main - test a function that print if the integer is positive or negative
 *
 * Return: End the program
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
