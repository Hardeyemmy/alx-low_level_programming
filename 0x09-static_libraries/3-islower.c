#include "main.h"
/**
 * _islower - functions that check for alx student
 *
 * @c: int parameter to the function
 *
 * Return: End the program
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}
