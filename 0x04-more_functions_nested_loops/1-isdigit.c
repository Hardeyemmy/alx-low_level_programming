#include "main.h"
/**
 * _isdigit - print 1 if it is a digit otherwise print 0
 *
 * Return: return value
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
